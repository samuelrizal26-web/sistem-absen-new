[build]
builder = "NIXPACKS"
buildCommand = "npm run build"
watchPaths = ["package.json", "vite.config.js", "src"]

[deploy]
startCommand = "npx serve dist -l $PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
